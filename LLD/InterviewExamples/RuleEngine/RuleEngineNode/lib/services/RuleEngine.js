export default class RuleEngine {

    // pass list of rules of trip and each expense and of each expense type
    evaluate(tripRules,expenses,expenseRuleRegistry,allExpenseRuleRegistry) {
        throw new Error("evaluate function is not defined")
    }
}